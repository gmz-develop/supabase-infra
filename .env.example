## Local app/SDK config (use local or remote below)
# For local stack started via `npx supabase start`:
# SUPABASE_URL=http://127.0.0.1:54321

# For remote project:
SUPABASE_URL=https://<PROJECT_REF>.supabase.co

# Client keys
SUPABASE_ANON_KEY=
# Never expose service role in frontend; used by Edge Functions or local tests only
SUPABASE_SERVICE_ROLE_KEY=

# Local DB connection for scripts/tests
DB_URL=postgresql://postgres:postgres@localhost:54322/postgres
